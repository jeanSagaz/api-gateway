apiVersion : apps/v1
kind: Deployment
metadata:
  name: api_gateway 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api_gateway
  template:
    metadata:
      labels:
        app: api_gateway
    spec:
      containers:
        - name: api_gateway 
          image: testazcr.azurecr.io/api_gateway
          ports:
          - containerPort: 8080
          env: 
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"